
@inject UserService UserService

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Fintrack</a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="@($"/dashboard/{u_id}")" Match="NavLinkMatch.All">
                <span class="" aria-hidden="true"></span> Transactions
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="cashin">
                <span class="" aria-hidden="true"></span> Cash In
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="cashout">
                <span class="" aria-hidden="true"></span> Cash Out
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="debt">
                <span class="" aria-hidden="true"></span> Debt
            </NavLink>
        </div>
    </nav>
</div>

@code{
    private int u_id { get; set; }
    protected override void OnInitialized()
    {
        u_id = UserService.getses();
    }
}